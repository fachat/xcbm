
#include	<stdint.h>

typedef		unsigned short int	scnt;
typedef		unsigned int		saddr;		// more than 16 bit
typedef 	unsigned char  		uchar;
typedef 	unsigned long		cnt;
typedef		unsigned char		byte;

typedef		uint64_t 		CLOCK;

#define	CLOCK_MAX	(~((CLOCK)0))

